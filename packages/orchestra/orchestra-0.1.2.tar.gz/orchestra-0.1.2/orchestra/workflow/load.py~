def load_workflow(workflow_metadata, versions=None):
    # Wrap this in a transaction

    # Pull out the workflow slug
    # Create the version if it doesn't exist

    # Create certifications if they don't exist, barf if they do

    # For each version in versions (or all versions if version is None):
    #   Create the version if it doesn't exist, barf if it does
    pass
