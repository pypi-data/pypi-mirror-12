#encoding:utf-8
from __future__ import unicode_literals
from setuptools import setup, find_packages

setup(
    name = 'SendRequest',
    version = '0.3',
    description = 'http request with retry, download pic.封装网络请求，自带重试',
    packages = find_packages()
)
