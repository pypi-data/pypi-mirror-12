#!/bin/sh
i=$1
  all=` cat /proc/cpuinfo |grep processor|wc -l`

  used=`ps ax -o %cpu|awk '{tmp=sprintf("%.0f",($1+20)/100);sum=sum + tmp}END{print sum}'`

  if [ $all -gt 0 ];then echo $i $used $all>>~/node_available;fi  
