* add unit tests for:
  - extracting the fp from an actual repo
  - `archive` command monkeypatching

* make `format` option support ``python:ASSET`` spec

* support multiple pathnames with multiple formats?

* auto-detect format from pathname? use mimetypes.guess_type?
