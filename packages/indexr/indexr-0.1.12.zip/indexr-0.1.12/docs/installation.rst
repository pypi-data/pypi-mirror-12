============
Installation
============

At the command line::

    $ easy_install indexr

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv indexr
    $ pip install indexr
